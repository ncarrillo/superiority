# Client resources

Fonts and images bundled with the desktop app.

- `fonts/`: bundled fonts
- `images/`: backgrounds, branding, controls, icons, and portraits

Rebuild prepared assets from the repository root:

```sh
./scripts/prepare-app-assets.zsh
```
