---
import Chapter from "../components/Chapter.astro";
import BackToContents from "../components/BackToContents.astro";
---

<Chapter id="terminology">
<h2>1. Names and protocol layers</h2>
  <p>StarCraft II's online path uses a protocol stack. This document uses the
  following names consistently:</p>
  <table class="terminology-table">
    <thead><tr><th>Name used here</th><th>Meaning</th></tr></thead>
    <tbody>
      <tr><td><b>Battle.net platform</b></td><td>The complete account, routing, session, and game-service system sometimes called Battle.net 2.0.</td></tr>
      <tr><td><b>Front</b></td><td>SC2's regional protobuf RPC connection. It authenticates the account and obtains the game-native handoff.</td></tr>
      <tr><td><b>Web challenge</b></td><td>The HTTPS account-login step requested during Front authentication. It returns a short-lived credential to the client.</td></tr>
      <tr><td><b>GameUtilities handoff</b></td><td>The Front RPC that returns the native endpoint, session material, identity data, and native logon response.</td></tr>
      <tr><td><b>Sunken</b></td><td>SC2's game-native service connection reached after Front. It carries bit-packed service records.</td></tr>
      <tr><td><b>BSN metadata</b></td><td>Build-time schema metadata embedded in SC2. Numeric type IDs resolve to structural records and optional human-readable names.</td></tr>
    </tbody>
  </table>
  <p>With these names established, the next chapter follows one session from the
  first Front connection through the Sunken service stream.</p>
  <BackToContents />
</Chapter>
